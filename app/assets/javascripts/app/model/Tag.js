Ext.define('CD.model.Tag', {
    extend: 'Ext.data.Model',
    alias: 'model.tag',

    requires: [
        'Ext.data.Field'
    ],

    fields: [
        {
            name: 'tag'
        }
    ]
});

